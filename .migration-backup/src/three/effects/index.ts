/**
 * Post-processing effects (bloom, chromatic aberration, vignette, etc.) live
 * here. Add `@react-three/postprocessing` when a scene needs it — kept out of
 * the dependency tree until then to protect bundle size. Intentionally empty.
 */
export {};
