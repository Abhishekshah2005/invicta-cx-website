/**
 * Composed 3D scenes live here — one file per scene (e.g. `hero-field.tsx`),
 * each rendered inside a `<Scene>` from `@/three/canvas`. Reserved for the
 * Gaming flagship and hero moments; intentionally empty until then.
 */
export {};
