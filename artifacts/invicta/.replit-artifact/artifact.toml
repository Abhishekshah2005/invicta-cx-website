kind = "web"
previewPath = "/"
title = "Invicta"
version = "1.0.0"
id = "artifacts/invicta"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24339

[services.development]
run = "pnpm --filter @workspace/invicta run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/invicta", "run", "build" ]
publicDir = "artifacts/invicta/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24339"
BASE_PATH = "/"
